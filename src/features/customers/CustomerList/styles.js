import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ECF2FF',
  },
  title: {
    textAlign: 'center',
    paddingVertical: 10,
    fontSize: 20,
  },
  subtitle: {
    textAlign: 'center',
    paddingVertical: 10,
    fontSize: 15,
  },
});
