import { Create } from "../../../features/customers/Create";
import { SafeAreaView, ScrollView } from "react-native";

export const CreateScreen = () => (
  <SafeAreaView>
    <ScrollView>
      <Create />
    </ScrollView>
  </SafeAreaView>
);
